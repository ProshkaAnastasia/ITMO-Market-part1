server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ProshkaAnastasia/ITMO-Market-part1
          default-label: micro-services
          search-paths: /config-repo