server:
  compression:
    enabled: true
    min-response-size: 1024

eureka:
  instance:
    prefer-ip-address: true

logging:
  level:
    root: WARN

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always